# Use this for frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: p2m
    tier: frontend
  ports:
  - port: 3000
    targetPort: 3000
  type: LoadBalancer # MUST be LoadBalancer for Playwright/Browser access