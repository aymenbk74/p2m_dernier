apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: LoadBalancer  # CRITICAL for Docker Desktop
  selector:
    tier: backend
  ports:
    - protocol: TCP
      port: 8000        # Port on your host (localhost:8000)
      targetPort: 8000  # Port inside the container